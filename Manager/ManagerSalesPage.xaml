<Page x:Class="KP.ManagerSalesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Продажи абонементов"
      Background="{StaticResource BackgroundBrush}">

    <Page.Resources>
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Width" Value="250"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
        </Style>

        <Style x:Key="DatePickerStyle" TargetType="DatePicker">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Width" Value="250"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{StaticResource InputBackgroundBrush}"/>
        </Style>
    </Page.Resources>

    <Grid>
        <StackPanel Margin="20">
            <!-- Заголовок и поиск -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="Продажи абонементов" Style="{StaticResource HeaderTextStyle}"/>
                <TextBox x:Name="SearchBox" Width="250" Margin="20,0,0,0" 
                         Style="{StaticResource TextBoxStyle}" Tag="Поиск по клиенту"/>
                <Button Content="Найти" Click="SearchSale_Click" 
                        Style="{StaticResource ButtonStyle}" Margin="10,0,0,0"/>
            </StackPanel>

            <!-- Таблица продаж -->
            <!-- Оберните DataGrid в ScrollViewer и установите фиксированную высоту -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" Height="300" Margin="0,20">
                <DataGrid x:Name="SalesGrid" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding SaleID}" Width="50"/>
                        <DataGridTextColumn Header="Клиент" Binding="{Binding ClientName}" Width="200"/>
                        <DataGridTextColumn Header="Абонемент" Binding="{Binding MembershipName}" Width="150"/>
                        <DataGridTextColumn Header="Дата продажи" 
                             Binding="{Binding SaleDate, StringFormat=dd.MM.yyyy}" Width="120"/>
                        <DataGridTextColumn Header="Стоимость" 
                             Binding="{Binding Price, StringFormat=C}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </ScrollViewer>

            <!-- Форма новой продажи -->
            <Border Style="{StaticResource AuthBorderStyle}" Margin="0,20">
                <StackPanel>
                    <TextBlock Text="Новая продажа" Style="{StaticResource BodyTextStyle}" FontWeight="Bold"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="Клиент:" Style="{StaticResource LabelTextStyle}"/>
                            <ComboBox x:Name="ClientCombo" DisplayMemberPath="FullName" Style="{StaticResource ComboBoxStyle}"/>

                            <TextBlock Text="Дата продажи:" Style="{StaticResource LabelTextStyle}" Margin="0,10,0,0"/>
                            <DatePicker x:Name="SaleDatePicker" Style="{StaticResource DatePickerStyle}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Абонемент:" Style="{StaticResource LabelTextStyle}"/>
                            <ComboBox x:Name="MembershipCombo" DisplayMemberPath="Name" Style="{StaticResource ComboBoxStyle}"/>

                            <TextBlock Text="Стоимость:" Style="{StaticResource LabelTextStyle}" Margin="0,10,0,0"/>
                            <TextBox x:Name="PriceBox" Style="{StaticResource TextBoxStyle}" IsReadOnly="True"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Кнопки управления -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
                <Button Content="Оформить" Click="AddSale_Click" Style="{StaticResource ButtonStyle}" Width="120"/>
                <Button Content="Отмена" Click="ClearForm_Click" Style="{StaticResource ButtonStyle}" Width="120" Margin="10,0"/>
                <Button Content="Назад" Click="BackButton_Click" Style="{StaticResource ButtonStyle}" Width="120"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>