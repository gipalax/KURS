<Page x:Class="KP.ServicesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="ServicesPage">

    <Grid Style="{StaticResource MainGridStyle}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Предоставленные услуги" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,20"/>

            <DataGrid x:Name="ServicesDataGrid" 
          AutoGenerateColumns="True"
          Style="{StaticResource DataGridStyle}" 
          ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
          Height="300" 
          Width="Auto" 
          IsReadOnly="True" 
          Margin="0,0,0,20"/>


            <Button Content="Назад" Click="BackToAdmin_Click" Style="{StaticResource ButtonStyle}" Width="200"/>
        </StackPanel>
    </Grid>
</Page>
